#include "parser.h"
#include <stdlib.h>

// program is the last element of the list
// so any additions we make should be after it
// If program is NULL then we are just starting out
// so we have to initialise program ourselves

